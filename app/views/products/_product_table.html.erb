<table class="table table-dark table-striped table-bordered align-middle">
    <thead>
      <tr class="table table-dark">
        <th scope="col">#</th>
        <th scope="col">Title</th>
        <th scope="col">Stock</th>
        <th scope="col">Brand</th>
        <th class="col text-center">Operations</th>
      </tr>
    </thead>
    <tbody>
      <% products.each_with_index do |product, index| %>
          <div id="<%= dom_id product %>">
            <tr>
                <th scope="row"><%= index + 1 %></th>
                <td><%= product.title %></td>
                <td><%= product.stock %></td>
                <td><%= product.brand.title %></td>
                <td class="d-flex justify-content-center prod-table-func">

                  <%= link_to '<i class="fa-solid fa-eye fa-2x"></i>'.html_safe, product, :class => "prod-table-show" %>  

                  <%= link_to '<i class="fa-solid fa-pen fa-2x"></i>'.html_safe, 
                    edit_product_path(product), method: :edit, :class => "prod-table-edit" 
                  %>

                  <%= link_to '<i class="fa-solid fa-trash fa-2x"></i>'.html_safe, 
                    product, data: { turbo_method: :delete }, :class => "prod-table-delete" 
                  %>

                  <%= button_to '<i class="fa-sharp fa-solid fa-cart-plus fa-2x"></i>'.html_safe, 
                    { :controller => "sales", :action => "add_product_to_sale", :product_id => product.id }, 
                    { :method => :post, :class => "prod-btn"} 
                  %>

                  <%= button_to '<i class="fa-sharp fa-solid fa-cart-shopping fa-2x"></i>'.html_safe, 
                    { :controller => "sales", :action => "remove_product_from_sale", :product_id => product.id }, 
                    { :method => :delete, :class => "prod-btn" } 
                  %>

                </td>
            </tr>
          </div> 
      <% end %>
    </tbody>
</table>

